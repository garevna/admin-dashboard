export function showMapWorkerDebugMessage (data) {
  // const { status, ...content } = data
  // console.group('MAP WORKER DEBUG MESSAGE')
  console.log('MAP WORKER DEBUG MESSAGE\n', data)
  // Object.keys(content).forEach(key => console.log(key, content[key]))
  // console.groupEnd('WORKER DEBUG MESSAGE')
}
